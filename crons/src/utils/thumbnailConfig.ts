/** Public thumbnail CDN bucket (JSON + SVG). */
export const THUMB_BUCKET = "thumbnails.abstractplay.com";

/** Metas skipped for prerender/SVG (JSON-only or known broken). */
export const THUMBNAIL_BROKEN_METAS: readonly string[] = [];
